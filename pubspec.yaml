name: monitoring_by_email
description: App para monitoramento de processos por email.
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.0.18
  flutter_local_notifications: ^12.0.0
  flutter_html: ^3.0.0
  workmanager: ^0.5.0
  flutter_appauth: ^2.0.0
  imap_client: ^1.0.0
  pop3_client: ^0.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
